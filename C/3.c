#include <stdio.h>
/*
int main()
{
    int i, n, res = 0, pres = 0, tres = 0;
    scanf("%d", &n);

    for (i=0; i < n; i++) {
        if ((i == 0) || (i == 1)) {
            res = 1;
            pres = 0;
        }
        tres = res;
        res = res + pres;
        pres = tres;
        printf("%u ", res);
    }
    return 0;
}
*/
